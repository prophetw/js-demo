function reduce<T>(
  accumulator: (prevValue: T, currentValue: T) => void,
  currentValue: T,
) {}

// https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/Reduce

describe(' Array reduce ', () => {
  test('', () => {})
})
